export function generateTheme(primaryColor, secondaryColor, tertiaryColor, backgroundColor, errorColor) {
    return {
        primaryColor:
        secondaryColor,
        tertiaryColor,
        backgroundColor,
        errorColor
    };

}